---
description: COG / critical-factors analysis (cross-cuts steps). Critical capabilities → requirements → vulnerabilities → decisive points.
argument-hint: [friendly|threat]
---

Run the `jpp-cog` skill.

Argument `$1` (optional): `friendly` or `threat`. If omitted, ask the planner.

Inputs: `01_mission_analysis/`, `context.md`.
Outputs: `cog/friendly.md` and/or `cog/threat.md`.
Doctrine: field guide §8 entire, with the §8.6 completion 5-question check.
