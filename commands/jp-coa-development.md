---
description: JPP Step 3 — COA Development. Creates one file per COA, each with sketch, who/what/where/when/why/how, COG focus, and the 5-validity-test results.
---

Run the `jpp-coa-development` skill (JPP Step 3).

Inputs: `01_mission_analysis/`, `cog/`, `coa_evaluation_criteria.md`.
Outputs: `02_coas/coa-NN-<short-name>.md` (one file per COA).
Doctrine: field guide §5.4 (5 validity tests), §7.2 objectives/effects/tasks, §7.5–7.7, §7.9, §13.3.
